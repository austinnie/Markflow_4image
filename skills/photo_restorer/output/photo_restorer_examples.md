# photo_restorer 使用示例

以下是从代码中提取的使用示例：

## skill

### PhotoRestorer

```python
# 创建实例
obj = PhotoRestorer(config)
```

```python
result = obj._setup_logging()
# 设置日志
result = obj._setup_config()
# 设置配置
result = obj.get_models()
# 获取所有支持的模型
```

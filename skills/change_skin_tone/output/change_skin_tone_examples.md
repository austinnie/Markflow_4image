# change_skin_tone 使用示例

以下是从代码中提取的使用示例：

## skill

### ChangeSkinTone

```python
# 创建实例
obj = ChangeSkinTone(config)
```

```python
result = obj._setup_logging()
result = obj._setup_config()
result = obj._find_model(model_name)
```

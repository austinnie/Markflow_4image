# fantasy_character 使用示例

以下是从代码中提取的使用示例：

## skill

### FantasyCharacter

```python
# 创建实例
obj = FantasyCharacter(config)
```

```python
result = obj._setup_logging()
# 设置日志
result = obj._setup_config()
# 设置默认配置
result = obj._find_model(model_name)
# 查找模型文件
```

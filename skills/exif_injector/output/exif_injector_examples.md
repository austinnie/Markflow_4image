# exif_injector 使用示例

以下是从代码中提取的使用示例：

## skill

### ExifInjector

```python
# 创建实例
obj = ExifInjector(config)
```

```python
result = obj._setup_logging()
result = obj._setup_config()
result = obj._find_exiftool()
# 查找 exiftool 路径
```
